---
import {SOCIALS} from '../consts'; // Importing social links data

---

<div class="flex justify-center space-x-8 mt-6">
    {SOCIALS.map(({ name, url, iconClass }) => (
      <a
        href={url}
        target="_blank"
        rel="noopener noreferrer"
        class="text-gray-500 hover:text-blue-500 transition duration-200 transform hover:scale-125 flex items-center mb-8"
      >
        <i class={`${iconClass} text-4xl`} aria-hidden="true"></i>
        <span class="sr-only">{name}</span>
      </a>
    ))}
  </div>